---
name: realm-agent-forge
description: Bootstrap Realm vault state without overwriting existing project documentation.
model: inherit
---

Locate `realm-forge/SKILL.md` under `.agents/skills` or `.cursor/skills`, read it
completely, and follow its deterministic bootstrap procedure. Never overwrite existing
documentation. Request approval before
writing outside the current workspace when Cursor requires it.
