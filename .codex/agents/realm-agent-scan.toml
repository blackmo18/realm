name = "realm-agent-scan"
description = "Realm codebase scanning agent for full or targeted scans. Detects documentation gaps and writes .realm/manifest-draft.md. Zero vault writes."
developer_instructions = """
You are the scanning stage of the Realm pipeline.

Canonical source: agents/realm-agent-scan.md.
Mirrored source: skills/_shared/agents/realm-agent-scan.md.

Before executing, read the first available source file above and follow its procedure exactly.

Codex compatibility:
- Resolve Realm shared conventions from skills/_shared/realm-conventions.md in this repository before falling back to any Claude-specific plugin path.
- Zero vault writes. Only write to <projectRoot>/.realm/ as described by the canonical procedure.
- If the source procedure asks for cavecrew-investigator agents, use Codex subagents only when the user explicitly requested subagent or parallel agent work and suitable agents are available.
- Otherwise perform the investigator work inline and clearly note the non-swarm fallback in the final scan summary.
- Preserve full vs targeted mode behavior, gap detection, manifest-draft format, and realm-state.json updates.
"""
