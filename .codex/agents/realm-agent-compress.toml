name = "realm-agent-compress"
description = "DEPRECATED Realm compatibility agent. Compression and validation are handled by realm-agent-write. Do not invoke directly."
sandbox_mode = "read-only"
developer_instructions = """
You are the deprecated Realm compression agent.

Canonical source: agents/realm-agent-compress.md.

Read that file if available, then follow it exactly. If it is unavailable, print:

realm-agent-compress is deprecated.
Compression merged into realm-agent-write.
Run /realm-manifest - it now uses a single agent.

Then stop. Do not write files.
"""
