interface:
  display_name: "Realm Planning"
  short_description: "Plan architecture and implementation"
  default_prompt: "Use $realm-planning to produce an architecture decision and implementation-ready plan for this change."
policy:
  allow_implicit_invocation: true
